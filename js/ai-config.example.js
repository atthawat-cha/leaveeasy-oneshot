// คัดลอกไฟล์นี้เป็น js/ai-config.js (ไฟล์นั้นถูกกันไว้ใน .gitignore แล้ว)
// แล้วใส่ API key ของคุณเองแทนค่าด้านล่าง — ห้ามใส่ค่าจริงในไฟล์นี้
// ⚠️ ห้ามใช้คีย์ OpenRouter ส่วนตัว ให้ใช้คีย์ที่ผู้สอนจัดสรรให้เท่านั้น
window.AI_CONFIG = {
  apiKey: "YOUR_OPENROUTER_API_KEY",
  model: "google/gemini-2.5-flash-lite"
};
