# BACKLOG.md — สิ่งที่ยังไม่เสร็จ (สัปดาห์ที่ 9)

> รายการนี้จดตามจริง ไม่ปรุงแต่งให้ดูดีกว่าที่เป็น — ตามกฎ "ห้ามแก้รายงานให้สวยกว่าผลจริง"

## 🔴 สำคัญ

- **ย้ายคีย์ AI ออกจากฝั่งที่ผู้ใช้แตะได้** — ตอนนี้ `js/ai-config.js` (ไม่ commit ขึ้น GitHub อยู่แล้ว) ยังเก็บ OpenRouter API key ไว้ในโค้ดฝั่งเบราว์เซอร์โดยตรง ใครก็เปิด DevTools ดูคีย์ได้ถ้ามีไฟล์นี้อยู่ในเครื่อง วิธีที่ถูกต้องกว่าคือย้ายการเรียก OpenRouter ไปไว้หลัง Cloud Function/Backend ที่ผู้ใช้แตะไม่ถึง แต่ Module 2 นี้ไม่มีเซิร์ฟเวอร์ที่เขียนเอง จึงทำแบบนั้นไม่ได้ในสโคปนี้

- **ปุ่ม "ส่งความเห็น" ในหน้ารายละเอียดใบลายังไม่เขียนลง Firestore จริง** (`js/leave-request-detail.js` ฟังก์ชัน `ส่งความเห็น`) — ยัง push เข้าหน่วยความจำเฉยๆ (ตกค้างจากโค้ดยุคก่อนล็อกอิน) และ hardcode ชื่อผู้เขียนเป็น "สมหญิง รักงาน" เสมอ ไม่ใช่คนที่ล็อกอินอยู่จริง ขัดกับ US-05 ที่ต้องเก็บลง subcollection `approvals` จริง — **ยังไม่ได้แก้ในรอบนี้เพราะนอกขอบเขตงานที่ตกลงไว้ (แก้เฉพาะ Security Rules ตามที่เทสต์ B4 พบ)**

## 🟡 ควรพิจารณา

- **ปุ่ม "ให้ AI ช่วยสรุปใบลาให้หัวหน้าอ่าน"** ใน `leave-request-detail.js` เป็นฟีเจอร์ที่ไม่ได้ระบุใน `leaveeasy-spec.md` (สเปคระบุแค่ US-09 ปุ่มจัดประเภทลาในหน้ายื่นใบลาใหม่) — ควรพิจารณาตัดออก หรือถ้าจะเก็บไว้ควรซ่อนปุ่มนี้จาก employee ในหน้าจอ เพราะหลังแก้ Security Rules แล้ว employee กดปุ่มนี้จะเจอ `permission-denied` (เขียน `aiSuggestion` ไม่ได้อีกต่อไป เพราะ `leaveRequests.update` จำกัดไว้เฉพาะ manager/hr)

- **`leaveTypes` เขียนได้โดยผู้ใช้ทุกบทบาท** — `firestore.rules` ปัจจุบันยังให้ authenticated user ทุกคนเพิ่ม/แก้/ลบประเภทการลาได้ (`allow read, write: if isSignedIn();`) ทั้งที่ `ACL.md` และสเปค (US-06) ระบุว่าต้องเป็นสิทธิ์ของ `hr` เท่านั้น — ยังไม่ได้แก้ในรอบนี้เพราะไม่ใช่จุดที่เทสต์ B4 พบว่ารั่ว (ไม่มี UI มอบหมายบทบาทหรือทดสอบส่วนนี้) แต่ควรแก้ต่อในรอบถัดไป

- **`leaveRequests.update` (สถานะ) ไม่เช็คว่าเปลี่ยนได้ครั้งเดียว** — Security Rules ปัจจุบันอนุญาตให้ manager/hr `updateDoc` ได้เสมอ ไม่ว่าสถานะเดิมจะเป็นอะไร (ฝั่ง UI ปิดปุ่มไว้เมื่อพิจารณาแล้ว แต่รูปแบบการยิง request ตรง ๆ ผ่าน console ยังทำได้) ควรเพิ่มเงื่อนไข `resource.data.status == 'รอพิจารณา'` ในกฎ `update` ด้วยเพื่อบังคับที่ชั้นข้อมูลจริง ไม่ใช่แค่ฝั่ง UI

## ⚪ ทำแล้ว ไม่ต้องทำต่อ

- Security Rules รายห้องครบชุดตาม `ACL.md` (leaveRequests + approvals + aiLog + users.role) — ทำในสัปดาห์นี้แล้ว ดู `test-results.md`
