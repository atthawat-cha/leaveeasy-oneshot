// คัดลอกไฟล์นี้เป็น config.js (ไฟล์นั้นถูกกันไว้ใน .gitignore แล้ว)
// แล้วใส่ API key ของคุณเองแทนค่าด้านล่าง — ห้ามใส่ค่าจริงในไฟล์นี้
window.OPENROUTER_CONFIG = {
  apiKey: "YOUR_OPENROUTER_API_KEY",
  model: "google/gemini-2.5-flash-lite"
};
