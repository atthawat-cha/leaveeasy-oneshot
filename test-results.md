# test-results.md — ผลการทดสอบ LeaveEasy (สัปดาห์ที่ 9)

> ทดสอบด้วย Playwright MCP (เปิดเบราว์เซอร์จริง ไม่ใช่ headless) บนเว็บที่ deploy จริง
> URL ที่ทดสอบ: https://leaveeasy-a17ab.web.app
> วันที่ทดสอบ: 22 กันยายน 2569 · เวลา 19:00–20:15 น.
> บัญชีทดสอบ: สร้างขึ้นเองสำหรับสัปดาห์นี้ (employee ×2, manager ×1) — ดูรายละเอียดใน `.claude/agents/tester.md`
>
> ⚠️ ห้ามแก้โค้ดเพื่อให้เทสต์ผ่าน — เทสต์ที่ 5 ด้านล่าง fail จริงในรอบแรก และมีการวิเคราะห์แล้วแก้ที่ต้นเหตุ (Security Rules) ไม่ใช่แก้เทสต์

## สรุปผล — ผ่านทั้งหมด 5/5 (หลังแก้ไข)

| # | เทสต์ | ผล | หมายเหตุ |
|---|---|:---:|---|
| 1 | ยื่นใบลาแล้วเห็นในรายการ (เส้นทางหลัก) | ✅ ผ่าน | รีเฟรชแล้วใบยังอยู่ — บันทึกจริงลง Firestore |
| 2 | กดอนุมัติแล้วสถานะเปลี่ยน (เส้นทางหลัก) | ✅ ผ่าน | รีเฟรชแล้วสถานะยังเป็น "อนุมัติ" · ปุ่มถูกปิดใช้งานถาวร |
| 3 | กรอกฟอร์มเว้นช่องหัวข้อไว้ → ต้องไม่บันทึก | ✅ ผ่าน | ขึ้นข้อความเตือน ไม่มีข้อมูลใหม่ถูกสร้าง |
| 4 | ไม่ล็อกอินแล้วเปิดหน้ารายการ → ต้องอ่านไม่ได้ | ✅ ผ่าน | ทั้ง UI (เด้งไปหน้าเข้าสู่ระบบ) และชั้นข้อมูล (`permission-denied`) |
| 5 | 🔒 บัญชี employee คนที่สอง เปิดใบลาของ employee คนแรก → ต้องเปิดไม่ได้ | ✅ ผ่าน (หลังแก้) | **รอบแรก FAIL จริง** — ดูหัวข้อ "ช่องโหว่ที่พบและการแก้ไข" ด้านล่าง |

## รายละเอียดแต่ละเทสต์

### เทสต์ 1 — ยื่นใบลาแล้วเห็นในรายการ
1. ล็อกอินบัญชี employee → เปิดหน้ายื่นใบลาใหม่
2. กรอกครบทุกช่อง → กดบันทึก → เด้งกลับหน้ารายการ เห็นใบใหม่สถานะ "รอพิจารณา"
3. กด F5 รีเฟรช → ใบยังอยู่ (ไม่ใช่ข้อมูลปลอมที่หายตอนรีเฟรช)

**ผล:** ✅ ผ่าน — ทดสอบเวลา 19:33 น.

### เทสต์ 2 — กดอนุมัติแล้วสถานะเปลี่ยน
1. ล็อกอินบัญชี manager → เปิดใบจากเทสต์ 1
2. กดปุ่ม "อนุมัติ" → สถานะเปลี่ยนเป็น "อนุมัติ" ทันทีบนหน้าจอ
3. รีเฟรชหน้า → สถานะยัง "อนุมัติ" (บันทึกจริง) · ปุ่มอนุมัติ/ไม่อนุมัติถูกปิดถาวร

**ผล:** ✅ ผ่าน — ทดสอบเวลา 19:34 น.

### เทสต์ 3 — กรอกฟอร์มไม่ครบ (เว้นหัวข้อ)
1. ล็อกอินบัญชี employee → เปิดหน้ายื่นใบลาใหม่
2. กรอกทุกช่องยกเว้นหัวข้อ → กดบันทึก

**ผล:** ✅ ผ่าน — ขึ้นข้อความ "⚠️ กรอกไม่ครบ — ต้องกรอกทุกช่องก่อนกดบันทึก" ไม่พาไปหน้ารายการ และจำนวนใบลาในระบบไม่เพิ่มขึ้น — ทดสอบเวลา 19:35 น.

### เทสต์ 4 — ไม่ล็อกอินแล้วอ่านข้อมูลไม่ได้
1. ออกจากระบบ (signOut)
2. พยายามเปิดหน้ารายการใบลาโดยตรง + พยายามอ่าน collection `leaveRequests` ตรง ๆ ผ่าน Firestore SDK

**ผล:** ✅ ผ่าน — UI เด้งไปหน้าเข้าสู่ระบบทันที และการอ่านข้อมูลตรง ๆ ได้ error `permission-denied` — ทดสอบเวลา 19:37 น. และ 20:12 น. (ทดสอบซ้ำหลังแก้ Security Rules)

### เทสต์ 5 — บัญชี employee คนที่สอง เปิดใบของ employee คนแรกไม่ได้ 🔒
1. ล็อกอินบัญชี employee คนที่สอง (คนละบัญชีกับผู้สร้างใบในเทสต์ 1)
2. เปิดใบลาของ employee คนแรกโดยตรงผ่าน URL (`leave-request-detail.html?id=...`)

**ผลรอบแรก (19:38 น.):** ❌ **ไม่ผ่าน** — เปิดเห็นข้อมูลเต็มทุกช่อง (หัวข้อ เหตุผล วันที่ ฯลฯ) ทั้งที่ควรเปิดไม่ได้

**ผลรอบสอง หลังแก้ไข (20:09 น.):** ✅ ผ่าน — ได้ error `permission-denied` ทั้งตอนเปิดใบโดยตรงและตอนโหลดหน้ารายการ (แสดงข้อความ "โหลดข้อมูลจาก Firestore ไม่สำเร็จ: Missing or insufficient permissions.")

## 🔴 ช่องโหว่ที่พบและการแก้ไข

**สาเหตุที่แท้จริง (วิเคราะห์ก่อนแก้ ตามกฎเหล็กของใบงาน):** เป็นโค้ดผิด ไม่ใช่เทสต์เขียนผิด — ยืนยันได้จาก 2 จุด:

1. `firestore.rules` (ก่อนแก้) บังคับแค่ `request.auth != null` — "ล็อกอินก็พอ" ไม่มีเงื่อนไขเช็คว่า `requesterId == request.auth.uid` เลย ตรงกับที่ `ACL.md` เตือนไว้เองว่า "ยังไม่มีการเช็คแยกตามบทบาทหรือความเป็นเจ้าของใบลาจริงในกฎ"
2. `js/leave-requests.js` (ก่อนแก้) query `collection(db,"leaveRequests")` แบบไม่กรอง ทำให้หน้ารายการของ employee เห็นใบของทุกคนตั้งแต่ก่อนกดเปิดรายละเอียดด้วยซ้ำ

ตรงกับ US-08 ในสเปคที่ระบุชัดว่า "ผู้ขอลาคนหนึ่งเปิดใบลาของผู้ขอลาอีกคนไม่ได้"

**การแก้ไข:**
1. เขียน `firestore.rules` ใหม่ทั้งไฟล์ตาม `ACL.md` — employee อ่าน/ลบได้เฉพาะใบของตัวเอง (ลบได้เฉพาะตอน `รอพิจารณา`), manager/hr อ่าน/แก้ไขได้ทุกใบ, สร้างใบใหม่ได้เฉพาะของตัวเองและต้องเริ่มที่ `รอพิจารณา` เท่านั้น
2. แก้ `js/leave-requests.js` ให้เช็ค role ของผู้ใช้ก่อน query: employee ใช้ `where("requesterId","==",uid)`, manager/hr query ทั้งหมด (Firestore ต้องเห็น `where` ตรงกับ rule ถึงจะอนุญาต list query ได้)
3. ระหว่างตั้งค่าเทสต์ยังพบช่องโหว่เพิ่มอีกจุด: บัญชีใด ๆ แก้ฟิลด์ `role` ของตัวเองเป็น `manager` ได้เองผ่าน Firestore ตรง ๆ — ปิดแล้วด้วยการเพิ่มเงื่อนไขใน `users.update` ว่าห้ามเปลี่ยนค่า `role`
4. Deploy `firestore.rules` และ hosting ขึ้น `leaveeasy-a17ab` จริงแล้ว และรันเทสต์ทั้ง 5 ตัวซ้ำจนผ่านหมด (ดูตารางด้านบน)

## รายชื่อบัญชีทดสอบที่ใช้

สร้างขึ้นเองสำหรับสัปดาห์นี้บน Firebase project `leaveeasy-a17ab` — อีเมล/รหัสผ่านจริงเก็บไว้ที่ `docs/workshop/Week8/week9/test-accounts.local.md` (ไฟล์ถูก `.gitignore` กัน ไม่ขึ้น GitHub)

| บทบาท | ใช้ในเทสต์ |
|---|---|
| employee (คนที่ 1) | เทสต์ 1, 2, 3, 5 (ผู้สร้างใบ) |
| employee (คนที่ 2) | เทสต์ 5 (ผู้พยายามเปิดใบของคนอื่น) |
| manager | เทสต์ 2 (ผู้อนุมัติ) |
